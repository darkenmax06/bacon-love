# ─── Base de datos ────────────────────────────────────────────────────────────
# PostgreSQL (Neon recomendado para Vercel)
# DATABASE_URL usa PgBouncer para serverless (pooled connection)
# DIRECT_URL es la conexión directa, usada solo para migraciones
DATABASE_URL="postgresql://usuario:password@host/basedatos?pgbouncer=true&connect_timeout=15"
DIRECT_URL="postgresql://usuario:password@host/basedatos?connect_timeout=15"

# ─── Autenticación ────────────────────────────────────────────────────────────
# Contraseña del panel de administración
ADMIN_PASSWORD="cambia-esta-contraseña"

# Clave secreta para firmar los JWT (genera una clave aleatoria larga)
JWT_SECRET="cambia-esta-clave-secreta-por-una-aleatoria-de-al-menos-32-caracteres"

# ─── SMTP — Correo electrónico ────────────────────────────────────────────────
# Si no se configura, los correos se omiten silenciosamente
# Para Gmail: usa una App Password (no tu contraseña normal)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_SECURE="false"
SMTP_USER="tu-correo@gmail.com"
SMTP_PASS="tu-app-password"
SMTP_FROM="Bacon Love <tu-correo@gmail.com>"
